const initialState = {
  fistname: '',
  lastname: '',
  password: '',
  email: '',
};

const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
